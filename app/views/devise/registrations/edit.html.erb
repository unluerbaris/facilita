<div class="container">
  <div class="row justify-content-center">
    <div class="col-8">
<div class="space"></div>
<h2>Edit <%= resource_name.to_s.humanize %></h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email, required: true, autofocus: true %>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
    <% end %>

<%= f.input :first_name,
                placeholder: "First name",
                required: false,
                input_html: { autocomplete: "first-name" } %>
<%= f.input :last_name,
                placeholder: "Last name",
                required: false,
                input_html: { autocomplete: "last-name" } %>
<%= f.input :birthday,
                start_year: Date.today.year - 100,
                end_year: Date.today.year,
                placeholder: "Birthday",
                required: false,
                input_html: { autocomplete: "birthday" } %>
<%= f.input :gender,
                as: :select,
                collection: ['Male', 'Female'],
                required: true,
                input_html: { autocomplete: "gender" } %>
<%= f.input :occupation,
                placeholder: "Occupation",
                required: false,
                input_html: { autocomplete: "occupation" } %>
<%= f.input :organization,
                placeholder: "Your organization name on Meetup.com",
                hint: "You can find your organization name on your organization page URL (https://www.meetup.com/<HERE>/)",
                required: false,
                input_html: { autocomplete: "organization" } %>
    <%= f.input :password,
                placeholder: "leave it blank if you don't want to change it",
                required: false,
                input_html: { autocomplete: "new-password" } %>
    <%= f.input :password_confirmation,
                required: false,
                input_html: { autocomplete: "new-password" } %>
    <%= f.input :current_password,
                placeholder: "we need your current password to confirm your changes",
                required: true,
                input_html: { autocomplete: "current-password" } %>
    <%= f.input :photo, as: :file, required: false %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Update", class: "btn btn-outline-danger mb-3" %>
  </div>
<% end %>

<h3>Cancel my account</h3>

<p>Unhappy? <%= link_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>

<%= link_to "Back", :back %>
<ul>
  <% @user.errors.full_messages.each do |message| %>
  <li><%= message %></li>
  <% end %>
</ul>
</div>
</div>
</div>
