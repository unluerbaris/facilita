<h1><%= @event.title %></h1>

<p><%= @event.description %></p>

<!-- Photo for later -->

<!-- Link to the creator -->


<div class="d-flex justify-content-around">
    <%= render 'messages', event: @event, message: @message %>
    <%= render 'questions', event: @event, question: @question %>
</div>

